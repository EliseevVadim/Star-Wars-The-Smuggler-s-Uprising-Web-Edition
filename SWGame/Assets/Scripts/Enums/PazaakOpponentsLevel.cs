namespace SWGame.Enums
{
    public enum PazaakOpponentsLevel
    {
        Easy, // dude uses only the classical cards (both types of them) 
        Medium, // dude uses all classical and flippable cards
        Hard // dude uses all game cards (in development)
    }
}
